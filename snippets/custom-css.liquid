<style>
@font-face {
  font-family: 'Poppins';
  font-style: normal;
  font-weight: 400;
  src: url(https://fonts.gstatic.com/s/poppins/v20/pxiEyp8kv8JHgFVrJJfecg.woff2) format('woff2');
  unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
}
.header__heading-logo{
    min-height:32px;
}
.hero-section4__img-box img {
    aspect-ratio: 3/2;
}
.product__info-wrapper .product-main.slick-initialized.slick-slider {
    margin-bottom: 30px;
}
.product2__variants .product-form__input:nth-last-child(2) {
    margin: 0;
}
.product2__slider .product-main.slick-initialized.slick-slider {
    aspect-ratio: 1;
}
.product2__slider .product-main > .item img {
    width: 100% !important;
    aspect-ratio: 1;
    height: auto !important;
}
@media (max-width: 575px){
.pdp-slider__wrapper-thumbs .swiper.pdp-thumbs-slider.mobile:not(.swiper-initialized.swiper-thumbs) .swiper-wrapper .swiper-slide {
    width: 20.8%;
    margin-right: 20px;
}}
.swiper.pdp-thumbs-slider:not(.swiper-initialized.swiper-thumbs) .swiper-slide{
    margin-right: 20px;
    width:104.75px;
}
.what-test-product__cols {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
}
.container {
    width: 100%;
    max-width: 114rem;
    margin: 0 auto;
    padding: 0 12px;
}
.badge {
    border: 1px solid transparent;
    border-radius: var(--badge-corner-radius);
    display: inline-block;
    font-size: 1.2rem;
    letter-spacing: .1rem;
    line-height: 1;
    padding: .6rem 1.3rem;
    text-align: center;
    background-color: rgb(var(--color-badge-background));
    border-color: rgba(var(--color-badge-border), var(--alpha-badge-border));
    color: rgb(var(--color-foreground));
    word-break: break-word
}
.card__badge.top.left .badge {
    border-radius: 0 var(--badge-corner-radius) var(--badge-corner-radius) 0;
}
body[data-template="collection"] .page-width.mcd_wrapper .mcd_moreinfo .mcd_moreinfo-review {
    min-height: 65px;
}

.price--on-sale .price__regular,
.price__sale {
    display: none
}

.price--on-sale .price__sale {
    display: initial;
    flex-direction: row;
    flex-wrap: wrap
}

.price--on-sale .price-item--regular {
    text-decoration: line-through;
    color: rgba(var(--color-foreground), .75);
    font-size: 1.3rem
}

@media screen and (min-width: 767px){
.prod2-review__slide-box {
    font-size: 12.5px;
    line-height: 24px;
}}
.video-welzo__poster {
    position: absolute;
    width: 100%;
    height: 100%;
}
.cart-count-bubble {
    position: absolute;
    background-color: rgb(var(--color-button));
    color: rgb(var(--color-button-text));
    height: 1.7rem;
    width: 1.7rem;
    border-radius: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: .9rem;
    bottom: 12px;
    left: 24px;
    line-height: calc(1 + .1 / var(--font-body-scale));
}
.product2-reviews__sld .slider-navigation {
    display: none;
}
.footer-disclaimer__image-item-link a.media.media--transparent img, img[src="https://static.legitscript.com/seals/14521644.png"] {
    display:none;
}
</style>